/*import { useState } from 'react';
import ReactMapGL from 'react-map-gl';

function Map() {

    const [viewport, setViewport] = useState({
        width: '100%',
        height: '100%',
        latitude: 9.913990,
        longitude: 8.889590,
        zoom: 11,
    });
    return (
        <ReactMapGL
        mapStyle='mapbox://styles/day-veed/cl94ejyij004215o46urqlreu'
        mapBoxApiAccessToken={process.env.mapbox_key}
        {...viewport}
        ></ReactMapGL>
    )
}

export default Map;*/